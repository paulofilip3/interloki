apiVersion: v1
kind: Service
metadata:
  name: {{ include "interloki.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "interloki.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "interloki.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
    - name: forward
      port: {{ .Values.service.forwardPort }}
      targetPort: forward
      protocol: TCP
